# Scripts

Safe helper scripts may be added here.

Rules:

- Do not store credentials in scripts.
- Prefer scripts that read configuration from environment variables or arguments.
- Document required inputs and expected outputs.
- Avoid scripts that mutate external services without explicit confirmation or a clear dry-run mode.
